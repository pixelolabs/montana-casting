<div class="insta-section">
  {% include 'icon-two-fish' %}
  <div class="container-1440">
    <div class="heading-section flex">
      {% if section.settings.username %}
      <h2>{{ section.settings.username }}</h2>
      {% endif %}
      {% if section.settings.insta_button %}
      <a class="link--primary" href="{{ section.settings.insta_url }}">{{ section.settings.insta_button }}</a>
      {% endif %}
    </div>
    <ul>
      {% for block in section.blocks %}
      <li>
        {% if block.settings.image != blank %}
        <a class="image-section">
          <img src="{{ block.settings.image | img_url: 'large' }}" alt="">
        </a>
        {% endif %}
      </li>
      {% endfor %}

    </ul>
  </div>
</div>

{% schema %}
{
  "name": "Instagram",
  "settings": [
    {
      "type": "text",
      "id": "username",
      "label": "Add instagram user name"
    },
    {
          "type": "text",
          "id": "insta_button",
          "label": "Instagram button title"
      },
    {
          "type": "url",
          "id": "insta_url",
          "label": "Instagram Url"
      }
  ],
  "max_blocks": 6,
  "blocks": [
     {
       "name": "Instagram",
       "type": "Instagram",
       "settings": [
         {
           "type": "image_picker",
           "id": "image",
           "label": "Image"
         }
       ]
     }
  ]
}
{% endschema %}
