<div class="hero-banner-slider">
  <div class="slider js__hero-banner-slider">
    <div class="swiper-wrapper">
      {% for block in section.blocks %}
          <div class="slide swiper-slide ">
            <div class="image-section">
              {% if block.settings.image != blank %}
              <picture>
                <source
                  media="(max-width: 600px)"
                  srcset="{{ block.settings.image | img_url: 'large' }}"
                >
                <img src="{{ block.settings.image | img_url: 'large'}}" alt="">
              </picture>
                {% endif %}
            </div>
            <div class="container-1280">
              <div class="content">
              {% if block.settings.heading %}
                <h2 class="h1">{{ blocks.settings.heading }}</h2>
              {% endif %}
              {% if block.settings.sub_heading %}
                <p>{{ block.settings.sub_heading }}</p>
              {% endif %}
              {% if block.settings.slid_button %}
                <a class="btn--primary" href="{{ section.settings.button_url }}">{{ block.settings.slid_button }}</a>
              {% endif %}
              </div>
            </div>
          </div>
        {% endfor %}
      
    </div>
    <div class="swiper-button-prev swiper-button-prev-hero-banner">
      {% include 'icon-arrow-prev' %}
    </div>
    <div class="swiper-button-next swiper-button-next-hero-banner">
      {% include 'icon-arrow-prev' %}
    </div>
  </div>
</div>



{% schema %}
  {
  "name": "Hero Banner",
    "blocks": [{
      "name": "Slides",
      "type": "slide",
        "settings": [
          {
            "type": "header",
            "content": "My settings"
          },
          {
            "type": "text",
            "id": "heading",
            "label": "Title",
            "default": "Enter title here"
          },
          {
            "type": "text",
            "id": "sub_heading",
            "label": "Description",
            "default": "Enter description here"
          },
          {
            "type": "text",
            "id": "slid_button",
            "label": "Button Text",
            "default": "Button"
          },
          {
              "type": "url",
              "id": "button_url",
              "label": "Button Link"
          },
          {
           "type": "image_picker",
           "id": "image",
           "label": "Slide Image"
         }
          
        ]
         
    }] 
      
}
{% endschema %}